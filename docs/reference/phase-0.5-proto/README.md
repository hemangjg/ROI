# Phase 0.5 Proto Reference (Archived)

These protobuf definitions were created during Phase 0.5 technical architecture.
They are **not active** in the Phase 1 Buf module.

Active contracts live in `contracts/proto/` (revived in P2-WS2).

These archived drafts were the source for the active domain protos. The active
versions may differ slightly (e.g. analytics RPC message names follow Buf STANDARD lint).