# Nx Generators

Custom Nx generators for scaffolding AI FinOps projects.

| Generator | Status | Purpose |
|-----------|--------|---------|
| `go-service` | WS5 | Scaffold a Go microservice with Chi, Wire, and health endpoints (manual scaffold via `scripts/scaffold-ws5-services.sh`) |
| `go-package` | WS4 | Scaffold a shared Go library module |

Generators will be implemented in later workstreams.