version: "2"
sql:
  - engine: postgresql
    schema: ../schemas
    queries: queries
    gen:
      go:
        package: sqlcgen
        out: gen
        sql_package: pgx/v5
        emit_json_tags: true
        emit_empty_slices: true
