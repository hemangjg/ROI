// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: workflow.sql

package sqlcgen

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const checkIdempotency = `-- name: CheckIdempotency :one
SELECT id, org_id, idempotency_key, event_id, created_at, expires_at
FROM idempotency_keys
WHERE org_id = $1 AND idempotency_key = $2 AND expires_at > now()
`

type CheckIdempotencyParams struct {
	OrgID          pgtype.UUID `json:"org_id"`
	IdempotencyKey string      `json:"idempotency_key"`
}

func (q *Queries) CheckIdempotency(ctx context.Context, arg CheckIdempotencyParams) (IdempotencyKey, error) {
	row := q.db.QueryRow(ctx, checkIdempotency, arg.OrgID, arg.IdempotencyKey)
	var i IdempotencyKey
	err := row.Scan(
		&i.ID,
		&i.OrgID,
		&i.IdempotencyKey,
		&i.EventID,
		&i.CreatedAt,
		&i.ExpiresAt,
	)
	return i, err
}

const recordIdempotency = `-- name: RecordIdempotency :one
INSERT INTO idempotency_keys (org_id, idempotency_key, event_id, expires_at)
VALUES ($1, $2, $3, $4)
RETURNING id, org_id, idempotency_key, event_id, created_at, expires_at
`

type RecordIdempotencyParams struct {
	OrgID          pgtype.UUID        `json:"org_id"`
	IdempotencyKey string             `json:"idempotency_key"`
	EventID        pgtype.UUID        `json:"event_id"`
	ExpiresAt      pgtype.Timestamptz `json:"expires_at"`
}

func (q *Queries) RecordIdempotency(ctx context.Context, arg RecordIdempotencyParams) (IdempotencyKey, error) {
	row := q.db.QueryRow(ctx, recordIdempotency,
		arg.OrgID,
		arg.IdempotencyKey,
		arg.EventID,
		arg.ExpiresAt,
	)
	var i IdempotencyKey
	err := row.Scan(
		&i.ID,
		&i.OrgID,
		&i.IdempotencyKey,
		&i.EventID,
		&i.CreatedAt,
		&i.ExpiresAt,
	)
	return i, err
}
